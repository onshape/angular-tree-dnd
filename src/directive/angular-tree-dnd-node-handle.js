angular.module('ntt.TreeDnD')    .directive(    'treeDndNodeHandle', function () {        return {            restrict: 'A',            scope:    true,            link:     function (scope, element, attrs) {                // expose scope in production mode                element[0].ngScope = function() {                    return scope;                };                element[0].querySelectorAll('*').forEach(function(elm) {                    elm.ngScope = function() {                        return scope;                    };                })                scope.$type = 'TreeDnDNodeHandle';                if (scope.$class.handle) {                    element.addClass(scope.$class.handle);                }            }        };    });